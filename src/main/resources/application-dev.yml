server:
  port: 8079
spring:
  datasource:
    url: jdbc:postgressql://localhost:5432/springData
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: true

